services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
      - "14550:14550/udp"   # C++ telemetry publisher → UDP listener (WP5)
    env_file:
      - ./configs/.env
    depends_on:
      timescaledb:
        condition: service_healthy
      redis:
        condition: service_healthy
      minio:
        condition: service_started
    volumes:
      - ./backend/app:/app
    networks:
      - drone-net

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    depends_on:
      - backend
    networks:
      - drone-net

  timescaledb:
    image: timescale/timescaledb:latest-pg16
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-drone_admin}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-drone_secret}
      POSTGRES_DB: ${POSTGRES_DB:-drone_station}
    volumes:
      - timescaledb_data:/var/lib/postgresql/data
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U drone_admin -d drone_station" ]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - drone-net

  redis:
    image: redis:7-alpine
    volumes:
      - redis_data:/data
    healthcheck:
      test: [ "CMD", "redis-cli", "ping" ]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - drone-net

  minio:
    image: minio/minio:latest
    ports:
      - "9000:9000"
      - "9001:9001"
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER:-minio_admin}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD:-minio_secret}
    command: server /data --console-address ":9001"
    volumes:
      - minio_data:/data
    networks:
      - drone-net

  # C++ telemetry publisher — run manually:
  #   docker compose --profile simulator up cpp_publisher
  cpp_publisher:
    build:
      context: ./cpp_telemetry_publisher
    depends_on:
      - backend
    environment:
      - TARGET_HOST=backend
      - TARGET_PORT=14550
    networks:
      - drone-net
    profiles:
      - simulator

volumes:
  timescaledb_data:
  redis_data:
  minio_data:


networks:
  drone-net:
    driver: bridge
